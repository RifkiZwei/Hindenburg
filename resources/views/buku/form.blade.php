<form class="panel" method="POST" enctype="multipart/form-data" action="{{ $buku ? route('buku.update',$buku) : route('buku.store') }}">@csrf @if($buku) @method('PATCH') @endif<div class="form-grid"><div class="field"><label>ISBN</label><input name="isbn" value="{{ old('isbn',$buku?->isbn) }}" required></div><div class="field"><label>Nama buku</label><input name="nama_buku" value="{{ old('nama_buku',$buku?->nama_buku) }}" required></div><div class="field"><label>Foto buku</label><input type="file" name="foto_buku" accept="image/jpeg,image/png,image/webp"></div><div class="field"><label>Stok</label><input type="number" min="0" name="stok" value="{{ old('stok',$buku?->stok ?? 0) }}" required></div><div class="field"><label>Kategori</label><select name="kategori_id" required><option value="">Pilih kategori</option>@foreach($kategoris as $kategori)<option value="{{ $kategori->id }}" @selected(old('kategori_id',$buku?->kategori_id)==$kategori->id)>{{ $kategori->nama_kategori }}</option>@endforeach</select></div></div><br><button class="button">Simpan</button></form>
