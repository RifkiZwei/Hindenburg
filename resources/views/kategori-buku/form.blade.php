<form class="panel" method="POST" enctype="multipart/form-data" action="{{ $kategori ? route('kategori-buku.update',$kategori) : route('kategori-buku.store') }}">@csrf @if($kategori) @method('PATCH') @endif<div class="field"><label>Genre</label><select name="nama_kategori" required><option value="">Pilih genre</option>@foreach($genres as $genre)<option value="{{ $genre }}" @selected(old('nama_kategori',$kategori?->nama_kategori)==$genre)>{{ $genre }}</option>@endforeach</select></div><br><div class="field"><label>Foto kategori</label><input type="file" name="foto_kategori" accept="image/jpeg,image/png,image/webp"></div><br><button class="button">Simpan</button></form>
